# Copy to .env and fill in. app.config.yaml substitutes these into the action inputs.
# Do NOT commit .env.

OKTA_ISSUER=https://adobe.okta.com/oauth2/aus1gan31wnmCPyB60h8
ORG=vidobe
REPO=nash
IMS_SCOPES=AdobeID,openid,read_organizations

# Service credential — choose ONE approach:
#   Recommended (auto-refreshing) — your App Builder project's OAuth Server-to-Server credential:
# IMS_CLIENT_ID=xxxxxxxx
# IMS_CLIENT_SECRET=xxxxxxxx
#   Quick test — a short-lived DA bearer token (from da.live console: window.adobeIMS.getAccessToken().token):
# DA_TOKEN=xxxxxxxx
